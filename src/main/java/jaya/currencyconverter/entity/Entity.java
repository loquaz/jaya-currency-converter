package jaya.currencyconverter.entity;

/**
 * This class was created to help database tables creation.
 * When the DataStorageModule starts db configuration,
 * we itarate over a set of entities (every model extends Entity)
 * and try create the table. 
 */
public abstract class Entity {
    
}
